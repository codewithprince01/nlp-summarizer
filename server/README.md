# NPL Clinic - Server

- Node.js + Express backend for authentication, report ingestion (text or image), OCR, and AI summarization.

## Setup

1. Copy `.env.example` to `.env` and fill values
2. Install deps
```
npm install
```
3. Start dev server
```
npm run dev
```

Health check: `GET /api/health`
